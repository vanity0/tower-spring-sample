apply plugin: 'java'
apply plugin: 'io.iamcyw.tower.Convention'

description("todo query")

dependencies {
    compile(project(':todo-domain'))

    implementation('io.iamcyw.tower:tower-messaging-core')

    implementation('org.springframework.boot:spring-boot-starter-data-jpa')
}